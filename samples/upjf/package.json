{
  "name": "upjf",
  "version": "1.0.0",
  "description": "U-Prove JSON Framework sample",
  "scripts": {
    "build": "tsc",
    "setup-issuer": "ts-node --files src/setup.ts",
    "deploy-issuer": "ts-node --files src/issuer.ts",
    "deploy-verifier": "ts-node --files src/verifier.ts",
    "prover": "ts-node --files src/prover.ts"
  },
  "license": "MIT",
  "dependencies": {
    "commander": "^10.0.0",
    "express": "^4.18.2",
    "express-rate-limit": "^6.7.0",
    "got": "^12.5.3",
    "jose": "^4.12.0",
    "ts-node": "^10.9.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.17"
  },
  "type": "module"
}
